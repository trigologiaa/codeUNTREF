package ejercicios

/*
La primera versión tiene la construcción del heap (heapify) que tiene una complejidad temporal O(n) y la ordenación (extraer y reconstruir el heap) que es O(n log n), y tiene una complejidad espacial O(1)

La segunda versión tiene la inserción en el heap que tiene una complejidad temporal (n log n) y la extracción con reconstrucción del heap O(n log n), y la complejidad espacial es O(n).

Ambas versiones tienen complejidad temporal O(n log n), y en cuanto a complejidad espacial la primera versión es más eficiente por ser O(1).
*/